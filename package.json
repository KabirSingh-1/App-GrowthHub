{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "PORT=8082 DATABASE_URL=${DATABASE_URL:-postgresql://postgres:postgres@localhost:5432/postgres} pnpm --filter '@workspace/api-server' run dev & PORT=8080 BASE_PATH=/ pnpm --filter '@workspace/appgrowth' run dev & PORT=8081 BASE_PATH=/ pnpm --filter '@workspace/mockup-sandbox' run dev & wait",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@esbuild/darwin-arm64": "0.27.3",
    "@rollup/rollup-darwin-arm64": "4.62.0",
    "@tailwindcss/oxide-darwin-arm64": "4.3.1",
    "lightningcss-darwin-arm64": "1.32.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "pnpm": {
    "supportedArchitectures": {
      "os": [
        "darwin"
      ],
      "cpu": [
        "arm64"
      ]
    }
  }
}
